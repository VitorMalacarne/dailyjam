<div class="dashboard-container">
  <h1>🎵 Nova Música</h1>
  
  <section>
    <%= form_with(model: @song, local: true, class: "song-form") do |form| %>
      <% if @song.errors.any? %>
        <div class="error-messages">
          <h3>⚠️ Erro ao salvar a música:</h3>
          <ul>
            <% @song.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= form.label :title, "Título *", class: "form-label" %>
        <%= form.text_field :title, class: "form-input", placeholder: "Ex: Imagine" %>
      </div>

      <div class="form-group">
        <%= form.label :artist, "Artista *", class: "form-label" %>
        <%= form.text_field :artist, class: "form-input", placeholder: "Ex: John Lennon" %>
      </div>

      <div class="form-group">
        <%= form.label :instrument, "Instrumento *", class: "form-label" %>
        <%= form.text_field :instrument, class: "form-input", placeholder: "Ex: Piano, Violão, Bateria" %>
      </div>

      <div class="form-group">
        <%= form.label :notes, "Notas (opcional)", class: "form-label" %>
        <%= form.text_area :notes, class: "form-textarea", rows: 4, 
                           placeholder: "Observações sobre a música, dificuldades, lembretes..." %>
      </div>

      <div class="form-actions">
        <%= form.submit "➕ Criar Música", class: "button button-primary" %>
        <%= link_to "❌ Cancelar", root_path, class: "button button-secondary" %>
      </div>
    <% end %>
  </section>
</div>